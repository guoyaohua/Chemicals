<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guoyaohua.chemicals">

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".tool_tool"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".ChemicalDetail" />
        <activity android:name=".Categories" />
        <activity android:name=".ChemicalDetailActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".IntroduceActivity" />
        <activity android:name=".PDFViewer"></activity>
    </application>

</manifest>